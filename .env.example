# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-a-strong-random-value
FLASK_ENV=development
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# Admin Credentials
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_strong_password_here

# Google Gemini API
GEMINI_API_KEY=your-gemini-api-key-here

# Instructions:
# 1. Copy this file to .env (never commit .env to git)
# 2. Generate a strong SECRET_KEY using: python -c "import secrets; print(secrets.token_hex(32))"
# 3. Set your admin credentials
# 4. Add your Gemini API key from https://makersuite.google.com/app/apikey
# 5. For production, set FLASK_ENV=production

# For Vercel Deployment:
# Add these environment variables in your Vercel project settings:
# - SECRET_KEY (required)
# - ADMIN_USERNAME (required)
# - ADMIN_PASSWORD (required)
# - GEMINI_API_KEY (required)
# - FLASK_ENV=production (recommended)