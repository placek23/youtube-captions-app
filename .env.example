# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-a-strong-random-value
FLASK_ENV=development
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# Admin Credentials
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_strong_password_here

# Google Gemini API
GEMINI_API_KEY=your-gemini-api-key-here

# YouTube Data API v3
YOUTUBE_API_KEY=your-youtube-api-key-here

# Vercel Postgres Database (Neon)
# These are automatically added by Vercel when you connect a Postgres database
# For local development, copy these values from your Vercel project settings
DATABASE_URL=your_postgres_connection_string
POSTGRES_URL=your_postgres_url
POSTGRES_PRISMA_URL=your_postgres_prisma_url
POSTGRES_URL_NON_POOLING=your_postgres_non_pooling_url
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_HOST=your_postgres_host
POSTGRES_DATABASE=your_postgres_database

# Instructions:
# 1. Copy this file to .env (never commit .env to git)
# 2. Generate a strong SECRET_KEY using: python -c "import secrets; print(secrets.token_hex(32))"
# 3. Set your admin credentials
# 4. Add your Gemini API key from https://makersuite.google.com/app/apikey
# 5. Add your YouTube API key from Google Cloud Console (enable YouTube Data API v3)
# 6. Add your Vercel Postgres connection details (from Vercel project settings)
# 7. For production, set FLASK_ENV=production

# For Vercel Deployment:
# Add these environment variables in your Vercel project settings:
# - SECRET_KEY (required)
# - ADMIN_USERNAME (required)
# - ADMIN_PASSWORD (required)
# - GEMINI_API_KEY (required)
# - YOUTUBE_API_KEY (required)
# - FLASK_ENV=production (recommended)
# - Postgres variables are automatically added when you connect a database